import { ObjectId } from "mongodb";

export default class{
    constructor(
        subscriptionName: string,
        dateAdded: Date,
        renewalDate: Date,
        chargeAmount: Number,
        category: string
    ){}
}